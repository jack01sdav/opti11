# Ultralytics YOLO 🚀, AGPL-3.0 license
# Multi-source heterogeneous Dataset (MSHD): HRSC2016 (1061) + HRSID (1061)
# The ratio of optical images to SAR images is 1:1.

# parent
# ├── ultralytics
# └── datasets
#     └── DOTA (Optical)
#     └── HRSC2016 (Optical)
#     └── HRSID (SAR)
#     └── SSDD (SAR)
#     └── MSHD (Optical&SAR)

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/MSHD
train: images/train
val: images/val
test: images/test
names:
  0: sar_ship
  1: opt_ship




